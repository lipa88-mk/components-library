import type { ArgTypes } from '@storybook/react';

// SkeletonRow takes no props — it is the primitive row used by SkeletonList and SkeletonCard.
export const argTypes: ArgTypes = {};

export const args = {};
